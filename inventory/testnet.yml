---
all:
  vars:
    env: testnet
  children:
    storyprotocol:
      hosts:
        bridge.storyprotocol.mainnet.encapsulate.xyz:
          type: bridge
        validator.storyprotocol.mainnet.encapsulate.xyz:
          type: validator
        fullnode.storyprotocol.mainnet.encapsulate.xyz:
          type: fullnode
